'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _enzyme = require('enzyme');

var _enzyme2 = _interopRequireDefault(_enzyme);

var _alertOctagon = require('./feather/alertOctagon');

var _SvgIcon = require('./SvgIcon');

var _enzymeAdapterReact = require('enzyme-adapter-react-16');

var _enzymeAdapterReact2 = _interopRequireDefault(_enzymeAdapterReact);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

_enzyme2.default.configure({ adapter: new _enzymeAdapterReact2.default() });

describe('SvgIcon test', function () {
    it('sets svg attribs to camelCase', function () {

        var svgIconEl = (0, _enzyme.shallow)(_react2.default.createElement(_SvgIcon.SvgIcon, { icon: _alertOctagon.alertOctagon }));
        //this icon has 3 fields needed to be camel cased, this should not have -

        var svgEl = svgIconEl.find('svg');

        var propsWithDash = Object.keys(svgEl.props()).filter(function (key) {
            return key.indexOf('-') >= 0;
        });

        //we should not find anything with a dash on it
        expect(propsWithDash.length).toBe(0);
    });
});